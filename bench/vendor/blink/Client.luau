--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--!nolint LocalUnused
--!nolint FunctionUnused
--#selene: allow(shadowing)
-- File generated by Blink v0.18.8 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

_G._BLINK = _G._BLINK or {}
if _G._BLINK["BLINK"] then
	error("[Blink]: An instance of blink is already running with the remote scope \"BLINK\". Change the remote scope of either instance to avoid conflicts.")
end
_G._BLINK["BLINK"] = true
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local SerdesFunctions = {}
local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local SerdesEvent = "?"
local SerdesThread: thread?
Queue.Reliable[1] = table.create(256)
Queue.Reliable[3] = table.create(256)
Queue.Reliable[5] = table.create(256)
export type Entity = { id: number, x: number, y: number, z: number, orientation: number, animation: number }
export type Flags12 = {f1: boolean,f2: boolean,f3: boolean,f4: boolean,f5: boolean,f6: boolean,f7: boolean,f8: boolean,f9: boolean,f10: boolean,f11: boolean,f12: boolean,}
export type Weapon = ("primary" | "secondary" | "melee" | "grenade")
export type Team = ("red" | "blue")
export type FlagIdiomatic = { flags: {f1: boolean,f2: boolean,f3: boolean,f4: boolean,f5: boolean,f6: boolean,f7: boolean,f8: boolean,f9: boolean,f10: boolean,f11: boolean,f12: boolean,}, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? }
export type FlagNaive = { f1: boolean, f2: boolean, f3: boolean, f4: boolean, f5: boolean, f6: boolean, f7: boolean, f8: boolean, f9: boolean, f10: boolean, f11: boolean, f12: boolean, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? }
function SerdesFunctions.WriteEVENT_ArrayHeavyUp(Value: { { id: number, x: number, y: number, z: number, orientation: number, animation: number } }): ()
	-- Allocate BLOCK: 1 bytes
	local BLOCK_START = Allocate(1)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 0)
	-- START ARRAY
		-- Allocate BLOCK: 6 bytes
	local ARRAY_START_1 = Allocate(6 * 100)
	for Index = 1, 100 do
		local Item_1 = Value[Index]
		if typeof(Item_1.id) ~= "number" then
			error(`Expected Item_1.id to be of type number, got {type(Item_1.id)} instead.`)
		end
		-- Allocate 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, Item_1.id)
		if typeof(Item_1.x) ~= "number" then
			error(`Expected Item_1.x to be of type number, got {type(Item_1.x)} instead.`)
		end
		-- Allocate 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_1, Item_1.x)
		if typeof(Item_1.y) ~= "number" then
			error(`Expected Item_1.y to be of type number, got {type(Item_1.y)} instead.`)
		end
		-- Allocate 1
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_2, Item_1.y)
		if typeof(Item_1.z) ~= "number" then
			error(`Expected Item_1.z to be of type number, got {type(Item_1.z)} instead.`)
		end
		-- Allocate 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_3, Item_1.z)
		if typeof(Item_1.orientation) ~= "number" then
			error(`Expected Item_1.orientation to be of type number, got {type(Item_1.orientation)} instead.`)
		end
		-- Allocate 1
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_4, Item_1.orientation)
		if typeof(Item_1.animation) ~= "number" then
			error(`Expected Item_1.animation to be of type number, got {type(Item_1.animation)} instead.`)
		end
		-- Allocate 1
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 1
		buffer.writeu8(SendBuffer, OPERATION_OFFSET_5, Item_1.animation)
	end
	-- END ARRAY
end

function SerdesFunctions.ReadEVENT_ArrayHeavyDown(): ({ { id: number, x: number, y: number, z: number, orientation: number, animation: number } })
	-- START ARRAY
	local Length = 100
	local Value = table.create(Length)
		-- Read BLOCK: 6 bytes
	local ARRAY_START_1 = Read(6 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 1
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.id = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0)
		-- Read 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.x = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 1
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.y = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.z = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_3)
		-- Read 1
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.orientation = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_4)
		-- Read 1
		local OPERATION_OFFSET_5 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.animation = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_5)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

function SerdesFunctions.WriteEVENT_FlagIdiomaticUp(Value: { flags: {f1: boolean,f2: boolean,f3: boolean,f4: boolean,f5: boolean,f6: boolean,f7: boolean,f8: boolean,f9: boolean,f10: boolean,f11: boolean,f12: boolean,}, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? }): ()
	-- Allocate BLOCK: 7 bytes
	local BLOCK_START = Allocate(7)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 2)
	local SET_BITS = 0
	if Value.flags.f1 == true then SET_BITS += 0x1 end
	if Value.flags.f2 == true then SET_BITS += 0x2 end
	if Value.flags.f3 == true then SET_BITS += 0x4 end
	if Value.flags.f4 == true then SET_BITS += 0x8 end
	if Value.flags.f5 == true then SET_BITS += 0x10 end
	if Value.flags.f6 == true then SET_BITS += 0x20 end
	if Value.flags.f7 == true then SET_BITS += 0x40 end
	if Value.flags.f8 == true then SET_BITS += 0x80 end
	if Value.flags.f9 == true then SET_BITS += 0x100 end
	if Value.flags.f10 == true then SET_BITS += 0x200 end
	if Value.flags.f11 == true then SET_BITS += 0x400 end
	if Value.flags.f12 == true then SET_BITS += 0x800 end
	buffer.writeu16(SendBuffer, BLOCK_START + 1, SET_BITS)
	if Value.weapon == "primary" then
		buffer.writeu8(SendBuffer, BLOCK_START + 3, 0)
	elseif Value.weapon == "secondary" then
		buffer.writeu8(SendBuffer, BLOCK_START + 3, 1)
	elseif Value.weapon == "melee" then
		buffer.writeu8(SendBuffer, BLOCK_START + 3, 2)
	elseif Value.weapon == "grenade" then
		buffer.writeu8(SendBuffer, BLOCK_START + 3, 3)
	else
		error(`Unexpected enum: {Value.weapon}, expected one of "primary" or "secondary" or "melee" or "grenade".`)
	end
	if Value.team == "red" then
		buffer.writeu8(SendBuffer, BLOCK_START + 4, 0)
	elseif Value.team == "blue" then
		buffer.writeu8(SendBuffer, BLOCK_START + 4, 1)
	else
		error(`Unexpected enum: {Value.team}, expected one of "red" or "blue".`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 5, Value.targetId ~= nil and 1 or 0)
	if Value.targetId ~= nil then
		-- Allocate BLOCK: 2 bytes
		local BLOCK_START = Allocate(2)
		if typeof(Value.targetId) ~= "number" then
			error(`Expected Value.targetId to be of type number, got {type(Value.targetId)} instead.`)
		end
		buffer.writeu16(SendBuffer, BLOCK_START + 0, Value.targetId)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.combo ~= nil and 1 or 0)
	if Value.combo ~= nil then
		-- Allocate BLOCK: 1 bytes
		local BLOCK_START = Allocate(1)
		if typeof(Value.combo) ~= "number" then
			error(`Expected Value.combo to be of type number, got {type(Value.combo)} instead.`)
		end
		buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.combo)
	end
end

function SerdesFunctions.ReadEVENT_FlagIdiomaticDown(): ({ flags: {f1: boolean,f2: boolean,f3: boolean,f4: boolean,f5: boolean,f6: boolean,f7: boolean,f8: boolean,f9: boolean,f10: boolean,f11: boolean,f12: boolean,}, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? })
	-- Read BLOCK: 6 bytes
	local BLOCK_START = Read(6)
	local Value = {} :: any
	Value.flags = {}
	local SET_BITS = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value.flags.f1 = bit32.btest(SET_BITS, 0x1);
	Value.flags.f2 = bit32.btest(SET_BITS, 0x2);
	Value.flags.f3 = bit32.btest(SET_BITS, 0x4);
	Value.flags.f4 = bit32.btest(SET_BITS, 0x8);
	Value.flags.f5 = bit32.btest(SET_BITS, 0x10);
	Value.flags.f6 = bit32.btest(SET_BITS, 0x20);
	Value.flags.f7 = bit32.btest(SET_BITS, 0x40);
	Value.flags.f8 = bit32.btest(SET_BITS, 0x80);
	Value.flags.f9 = bit32.btest(SET_BITS, 0x100);
	Value.flags.f10 = bit32.btest(SET_BITS, 0x200);
	Value.flags.f11 = bit32.btest(SET_BITS, 0x400);
	Value.flags.f12 = bit32.btest(SET_BITS, 0x800);
	Value.weapon = true :: any
	local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 2)
	if Index == 0 then
		Value.weapon = "primary"
	elseif Index == 1 then
		Value.weapon = "secondary"
	elseif Index == 2 then
		Value.weapon = "melee"
	elseif Index == 3 then
		Value.weapon = "grenade"
	else
		error(`Unexpected enum: {Index}`)
	end
	Value.team = true :: any
	Index = buffer.readu8(RecieveBuffer, BLOCK_START + 3)
	if Index == 0 then
		Value.team = "red"
	elseif Index == 1 then
		Value.team = "blue"
	else
		error(`Unexpected enum: {Index}`)
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 4) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		Value.targetId = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 5) == 1 then
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		Value.combo = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
	return Value
end

function SerdesFunctions.WriteEVENT_FlagNaiveUp(Value: { f1: boolean, f2: boolean, f3: boolean, f4: boolean, f5: boolean, f6: boolean, f7: boolean, f8: boolean, f9: boolean, f10: boolean, f11: boolean, f12: boolean, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? }): ()
	-- Allocate BLOCK: 17 bytes
	local BLOCK_START = Allocate(17)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 4)
	if typeof(Value.f1) ~= "boolean" then
		error(`Expected Value.f1 to be of type boolean, got {type(Value.f1)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value.f1 and 1 or 0)
	if typeof(Value.f2) ~= "boolean" then
		error(`Expected Value.f2 to be of type boolean, got {type(Value.f2)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Value.f2 and 1 or 0)
	if typeof(Value.f3) ~= "boolean" then
		error(`Expected Value.f3 to be of type boolean, got {type(Value.f3)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 3, Value.f3 and 1 or 0)
	if typeof(Value.f4) ~= "boolean" then
		error(`Expected Value.f4 to be of type boolean, got {type(Value.f4)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 4, Value.f4 and 1 or 0)
	if typeof(Value.f5) ~= "boolean" then
		error(`Expected Value.f5 to be of type boolean, got {type(Value.f5)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 5, Value.f5 and 1 or 0)
	if typeof(Value.f6) ~= "boolean" then
		error(`Expected Value.f6 to be of type boolean, got {type(Value.f6)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.f6 and 1 or 0)
	if typeof(Value.f7) ~= "boolean" then
		error(`Expected Value.f7 to be of type boolean, got {type(Value.f7)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 7, Value.f7 and 1 or 0)
	if typeof(Value.f8) ~= "boolean" then
		error(`Expected Value.f8 to be of type boolean, got {type(Value.f8)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.f8 and 1 or 0)
	if typeof(Value.f9) ~= "boolean" then
		error(`Expected Value.f9 to be of type boolean, got {type(Value.f9)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 9, Value.f9 and 1 or 0)
	if typeof(Value.f10) ~= "boolean" then
		error(`Expected Value.f10 to be of type boolean, got {type(Value.f10)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 10, Value.f10 and 1 or 0)
	if typeof(Value.f11) ~= "boolean" then
		error(`Expected Value.f11 to be of type boolean, got {type(Value.f11)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 11, Value.f11 and 1 or 0)
	if typeof(Value.f12) ~= "boolean" then
		error(`Expected Value.f12 to be of type boolean, got {type(Value.f12)} instead.`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 12, Value.f12 and 1 or 0)
	if Value.weapon == "primary" then
		buffer.writeu8(SendBuffer, BLOCK_START + 13, 0)
	elseif Value.weapon == "secondary" then
		buffer.writeu8(SendBuffer, BLOCK_START + 13, 1)
	elseif Value.weapon == "melee" then
		buffer.writeu8(SendBuffer, BLOCK_START + 13, 2)
	elseif Value.weapon == "grenade" then
		buffer.writeu8(SendBuffer, BLOCK_START + 13, 3)
	else
		error(`Unexpected enum: {Value.weapon}, expected one of "primary" or "secondary" or "melee" or "grenade".`)
	end
	if Value.team == "red" then
		buffer.writeu8(SendBuffer, BLOCK_START + 14, 0)
	elseif Value.team == "blue" then
		buffer.writeu8(SendBuffer, BLOCK_START + 14, 1)
	else
		error(`Unexpected enum: {Value.team}, expected one of "red" or "blue".`)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 15, Value.targetId ~= nil and 1 or 0)
	if Value.targetId ~= nil then
		-- Allocate BLOCK: 2 bytes
		local BLOCK_START = Allocate(2)
		if typeof(Value.targetId) ~= "number" then
			error(`Expected Value.targetId to be of type number, got {type(Value.targetId)} instead.`)
		end
		buffer.writeu16(SendBuffer, BLOCK_START + 0, Value.targetId)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 16, Value.combo ~= nil and 1 or 0)
	if Value.combo ~= nil then
		-- Allocate BLOCK: 1 bytes
		local BLOCK_START = Allocate(1)
		if typeof(Value.combo) ~= "number" then
			error(`Expected Value.combo to be of type number, got {type(Value.combo)} instead.`)
		end
		buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.combo)
	end
end

function SerdesFunctions.ReadEVENT_FlagNaiveDown(): ({ f1: boolean, f2: boolean, f3: boolean, f4: boolean, f5: boolean, f6: boolean, f7: boolean, f8: boolean, f9: boolean, f10: boolean, f11: boolean, f12: boolean, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? })
	-- Read BLOCK: 16 bytes
	local BLOCK_START = Read(16)
	local Value = {} :: any
	Value.f1 = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
	Value.f2 = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	Value.f3 = (buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1)
	Value.f4 = (buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1)
	Value.f5 = (buffer.readu8(RecieveBuffer, BLOCK_START + 4) == 1)
	Value.f6 = (buffer.readu8(RecieveBuffer, BLOCK_START + 5) == 1)
	Value.f7 = (buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1)
	Value.f8 = (buffer.readu8(RecieveBuffer, BLOCK_START + 7) == 1)
	Value.f9 = (buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1)
	Value.f10 = (buffer.readu8(RecieveBuffer, BLOCK_START + 9) == 1)
	Value.f11 = (buffer.readu8(RecieveBuffer, BLOCK_START + 10) == 1)
	Value.f12 = (buffer.readu8(RecieveBuffer, BLOCK_START + 11) == 1)
	Value.weapon = true :: any
	local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 12)
	if Index == 0 then
		Value.weapon = "primary"
	elseif Index == 1 then
		Value.weapon = "secondary"
	elseif Index == 2 then
		Value.weapon = "melee"
	elseif Index == 3 then
		Value.weapon = "grenade"
	else
		error(`Unexpected enum: {Index}`)
	end
	Value.team = true :: any
	Index = buffer.readu8(RecieveBuffer, BLOCK_START + 13)
	if Index == 0 then
		Value.team = "red"
	elseif Index == 1 then
		Value.team = "blue"
	else
		error(`Unexpected enum: {Index}`)
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 14) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		Value.targetId = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 15) == 1 then
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		Value.combo = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
	return Value
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
	ArrayHeavyUp = {
		Fire = NOOP
	},
	ArrayHeavyDown = {
		On = NOOP
	},
	FlagIdiomaticUp = {
		Fire = NOOP
	},
	FlagIdiomaticDown = {
		On = NOOP
	},
	FlagNaiveUp = {
		Fire = NOOP
	},
	FlagNaiveDown = {
		On = NOOP
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsClient() then
    error("Client network module can only be required from the client.")
end

local Reliable: RemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent

local function StepReplication()
    if SendCursor <= 0 then
        return
    end

    local Buffer = buffer.create(SendCursor)
    buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
    Reliable:FireServer(Buffer, SendInstances)

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    table.clear(SendInstances)
end
RunService.Heartbeat:Connect(StepReplication)
Reliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 1 then
			local Value: { { id: number, x: number, y: number, z: number, orientation: number, animation: number } } = SerdesFunctions.ReadEVENT_ArrayHeavyDown()
			if Events.Reliable[1] ~= nil then
				SerdesEvent = "ArrayHeavyDown"
				Events.Reliable[1](Value)
			else
				if #Queue.Reliable[1] > 256 then
					warn("[Blink]: Event queue of \"ArrayHeavyDown\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[1], {Value} :: {any})
			end
		elseif Index == 3 then
			local Value: { flags: {f1: boolean,f2: boolean,f3: boolean,f4: boolean,f5: boolean,f6: boolean,f7: boolean,f8: boolean,f9: boolean,f10: boolean,f11: boolean,f12: boolean,}, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? } = SerdesFunctions.ReadEVENT_FlagIdiomaticDown()
			if Events.Reliable[3] ~= nil then
				SerdesEvent = "FlagIdiomaticDown"
				Events.Reliable[3](Value)
			else
				if #Queue.Reliable[3] > 256 then
					warn("[Blink]: Event queue of \"FlagIdiomaticDown\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[3], {Value} :: {any})
			end
		elseif Index == 5 then
			local Value: { f1: boolean, f2: boolean, f3: boolean, f4: boolean, f5: boolean, f6: boolean, f7: boolean, f8: boolean, f9: boolean, f10: boolean, f11: boolean, f12: boolean, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? } = SerdesFunctions.ReadEVENT_FlagNaiveDown()
			if Events.Reliable[5] ~= nil then
				SerdesEvent = "FlagNaiveDown"
				Events.Reliable[5](Value)
			else
				if #Queue.Reliable[5] > 256 then
					warn("[Blink]: Event queue of \"FlagNaiveDown\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[5], {Value} :: {any})
			end
		end
	end
end)
Unreliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	ArrayHeavyUp = {
		Fire = function(Value: { { id: number, x: number, y: number, z: number, orientation: number, animation: number } }): ()
			SerdesFunctions.WriteEVENT_ArrayHeavyUp(Value)
		end
	},
	ArrayHeavyDown = {
		On = function(Listener: (Value: { { id: number, x: number, y: number, z: number, orientation: number, animation: number } }) -> ()): () -> ()
			Events.Reliable[1] = Listener
			for Index, Arguments in Queue.Reliable[1] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[1] = {}
			return function (): ()
				if Events.Reliable[1] == Listener then
					Events.Reliable[1] = nil
				end
			end
		end
	},
	FlagIdiomaticUp = {
		Fire = function(Value: { flags: {f1: boolean,f2: boolean,f3: boolean,f4: boolean,f5: boolean,f6: boolean,f7: boolean,f8: boolean,f9: boolean,f10: boolean,f11: boolean,f12: boolean,}, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? }): ()
			SerdesFunctions.WriteEVENT_FlagIdiomaticUp(Value)
		end
	},
	FlagIdiomaticDown = {
		On = function(Listener: (Value: { flags: {f1: boolean,f2: boolean,f3: boolean,f4: boolean,f5: boolean,f6: boolean,f7: boolean,f8: boolean,f9: boolean,f10: boolean,f11: boolean,f12: boolean,}, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? }) -> ()): () -> ()
			Events.Reliable[3] = Listener
			for Index, Arguments in Queue.Reliable[3] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[3] = {}
			return function (): ()
				if Events.Reliable[3] == Listener then
					Events.Reliable[3] = nil
				end
			end
		end
	},
	FlagNaiveUp = {
		Fire = function(Value: { f1: boolean, f2: boolean, f3: boolean, f4: boolean, f5: boolean, f6: boolean, f7: boolean, f8: boolean, f9: boolean, f10: boolean, f11: boolean, f12: boolean, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? }): ()
			SerdesFunctions.WriteEVENT_FlagNaiveUp(Value)
		end
	},
	FlagNaiveDown = {
		On = function(Listener: (Value: { f1: boolean, f2: boolean, f3: boolean, f4: boolean, f5: boolean, f6: boolean, f7: boolean, f8: boolean, f9: boolean, f10: boolean, f11: boolean, f12: boolean, weapon: ("primary" | "secondary" | "melee" | "grenade"), team: ("red" | "blue"), targetId: number?, combo: number? }) -> ()): () -> ()
			Events.Reliable[5] = Listener
			for Index, Arguments in Queue.Reliable[5] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[5] = {}
			return function (): ()
				if Events.Reliable[5] == Listener then
					Events.Reliable[5] = nil
				end
			end
		end
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL