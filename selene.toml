std = "roblox+netweave"

[lints]
# Adapters intentionally return tables whose fields are typed as optional in the
# Adapter interface; selene cannot see through the type annotation.
mixed_table = "allow"
